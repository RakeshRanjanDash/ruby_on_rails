
<%= link_to("Back to List", subjects_path, :class => 'back link') 
%>

<table summary="Subject detail view">
        <tr>
            <th> Name </th>
            <td> <%= @subject.name %> </th>
        </tr>
        <tr>
            <th> Position </th>
            <td> <%= @subject.position %> </th>
        </tr>
        <tr>
            <th> Visible </th>
            <td> <%= @subject.visible ? 'Yes' : 'No' %> </th>
        </tr>
    
    </table> 
